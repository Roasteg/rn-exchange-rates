class Currency {
    Code: string;
    Symbol: string;
    Flag: string;
    constructor(code: string, symbol: string, flag: string) {
        this.Code = code;
        this.Symbol = symbol;
        this.Flag = flag;
    }
}